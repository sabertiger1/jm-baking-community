# 经贸烘焙社区

> 基于 Mealie 改造的智慧化烘焙社区平台，专为学校学生设计的烘焙作品分享与交流系统

## 📖 项目简介

经贸烘焙社区是一个面向学校学生的智慧化烘焙社区平台，基于 [Mealie](https://github.com/mealie-recipes/mealie) 开源项目改造而成。该系统为学生提供了一个展示烘焙作品、分享制作心得、互相学习交流的平台。

### 核心功能

- 🎂 **食谱管理**：完整的食谱创建、编辑、浏览功能
- 📸 **作品展示**：学生可以提交自己的烘焙作品，展示制作成果
- ⭐ **评分评论**：对食谱进行1-5星评分和文字评论
- 🌸 **互动投票**：为喜欢的作品送花或送鸡蛋（消耗积分）
- 📅 **每日签到**：每日签到获得积分，连续签到有额外奖励
- 🏆 **排行榜**：积分排行榜、鲜花榜、鸡蛋榜
- 👥 **班级管理**：按班级、小组筛选和查看作品
- 👤 **个人中心**：查看个人作品、积分、签到记录等

## 🎯 项目特色

### 1. 用户角色管理
- **学生**：可以查看食谱、评论、上传作品、投票
- **老师**：可以创建和管理食谱，查看所有班级信息
- **管理员**：拥有所有权限

### 2. 资料完善机制
- 强制要求用户完善个人资料（真实姓名、年级、班级、头像）
- 未完善资料的用户功能受限
- 首次登录强制弹窗完善资料

### 3. 积分系统
- 每日签到获得10积分
- 送花/送鸡蛋消耗5积分
- 积分仅用于社区互动，不兑换实物

### 4. 作品管理
- 每个用户对每个食谱只能提交一次作品
- 支持图片上传和制作心得
- 作品支持按班级、小组筛选和排序

### 5. 评分系统
- 1-5星评分 + 文字评论
- 每个用户对每个食谱只能评论一次
- 评论不可删除，但可以修改

## 🛠️ 技术栈

### 后端
- **Python 3.11+**
- **FastAPI** - 现代、快速的Web框架
- **SQLAlchemy** - ORM框架
- **Alembic** - 数据库迁移工具
- **PostgreSQL/SQLite** - 数据库

### 前端
- **Nuxt 3** - Vue.js框架
- **Vue 3** - 渐进式JavaScript框架
- **Vuetify 3** - Material Design组件库
- **TypeScript** - 类型安全的JavaScript
- **Vue I18n** - 国际化支持（默认中文）

### 数据库设计

#### 新增数据表
1. **user_baking_records** - 用户烘焙作品记录
2. **recipe_ratings** - 食谱评分和评论
3. **user_points** - 用户积分系统
4. **work_votes** - 作品投票记录
5. **user_classes** - 用户班级和小组信息
6. **user_details** - 用户详细资料（真实姓名、年级、班级、头像）

## 📁 项目结构

```
mealie/
├── mealie/                    # 后端代码
│   ├── db/models/            # 数据库模型
│   │   ├── baking/           # 烘焙社区相关模型
│   │   └── users/            # 用户相关模型
│   ├── routes/               # API路由
│   │   ├── baking/          # 烘焙社区路由
│   │   └── users/            # 用户路由
│   ├── schema/               # Pydantic Schemas
│   │   ├── baking/          # 烘焙社区Schemas
│   │   └── user/            # 用户Schemas
│   └── alembic/versions/    # 数据库迁移脚本
│
├── frontend/                  # 前端代码
│   ├── components/           # Vue组件
│   │   └── Domain/Baking/   # 烘焙社区组件
│   ├── pages/                # 页面
│   │   └── baking/          # 烘焙社区页面
│   ├── lib/api/              # API客户端
│   │   └── user/            # 用户API
│   └── lang/messages/       # 国际化文件
│
└── 项目介绍.md               # 本文件
```

## 🚀 快速开始

### 环境要求

- Python 3.11+
- Node.js 18+
- PostgreSQL 12+ (或 SQLite)

### 安装步骤

1. **克隆项目**
```bash
git clone <your-repo-url>
cd mealie
```

2. **安装后端依赖**
```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

3. **安装前端依赖**
```bash
cd frontend
npm install  # 或 yarn install
```

4. **配置环境变量**
```bash
# 复制环境变量文件
cp .env.example .env
# 编辑 .env 文件，配置数据库等信息
```

5. **运行数据库迁移**
```bash
# 在项目根目录
alembic upgrade head
```

6. **启动开发服务器**

后端：
```bash
# 在项目根目录
uvicorn mealie.main:app --reload
```

前端：
```bash
# 在 frontend 目录
npm run dev  # 或 yarn dev
```

## 📝 主要功能说明

### 1. 食谱管理
- 老师和管理员可以创建、编辑食谱
- 支持添加制作视频和要点视频
- 学生可以查看食谱详情

### 2. 作品提交
- 在食谱详情页可以提交作品
- 每个用户对每个食谱只能提交一次
- 支持上传图片和填写制作心得

### 3. 评分评论
- 在食谱详情页可以评分和评论
- 每个用户对每个食谱只能评论一次
- 可以修改自己的评论

### 4. 投票系统
- 在作品集页面可以为作品送花或送鸡蛋
- 每个用户对每个作品每种类型只能投票一次
- 投票消耗5积分

### 5. 签到系统
- 每日签到获得10积分
- 连续签到有额外奖励
- 在个人主页查看签到记录

### 6. 排行榜
- 积分排行榜：按总积分排序
- 鲜花榜：按收到鲜花数排序
- 鸡蛋榜：按收到鸡蛋数排序

## 🔧 配置说明

### 用户角色
系统默认有三种角色：
- `student` - 学生（默认）
- `teacher` - 老师
- `admin` - 管理员

### 权限说明
- **学生**：只能查看、评论、上传作品、投票
- **老师**：可以创建食谱，查看所有班级信息
- **管理员**：拥有所有权限

### 资料完善
- 用户首次登录需要完善资料
- 必须填写：真实姓名、年级、班级、上传头像
- 未完善资料的用户功能受限

## 📄 许可证

本项目基于 Mealie 项目改造，遵循原项目的许可证。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题或建议，请通过 GitHub Issues 联系。

## 🙏 致谢

- [Mealie](https://github.com/mealie-recipes/mealie) - 原始项目
- 所有贡献者和使用者

---

**注意**：本项目为教育用途，请遵守相关法律法规和学校规定。
